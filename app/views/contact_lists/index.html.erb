

<table class="table">
  <thead>
    <tr>
      <th>Fecha de Lista</th>
      <th>Nombre</th>
      <th>Numero</th>
      <th>Respuesta</th>
    </tr>
  </thead>
  <tbody>
    <%@lists.each do |list|%>
      <% list.contacts.each do |contact|%>
        <tr>
          <th scope="row"><%=list.created_at.strftime("%d-%b-%y %I:%M%P")%></th>
          <td><%=contact.name%></td>
          <td><%=contact.phone%></td>
          <td><%=@client.calls.get(contact.sid)%></td>
          <td><%=decode_call_response(contact.response)%></td>
        </tr>
      <%end%>
    <%end%>
  </tbody>
</table>
